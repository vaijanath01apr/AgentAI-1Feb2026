To start MCP server > uvicorn mcp_server:app --host 0.0.0.0 --port 8000


uvicorn api:app --reload --port 8000   

Postman
    > Body
    > Raw
    > JSON
    { "message" : "If you are familiar with one or more other programming language, features should be familiar and known to you. All programming languages has" }